## creating a button class (context)
Can you create a btn class and styles using the theme colors in @app/globals.css. Then apply that class to the link in the navbar.
---

## creating a skeleton component (image contexy)
Can you make a new Skeleton UI component for later use? Base the design on this image, but use colours to match this project: '/Users/shaun/Code/Sandbox/claude-pocket-heist/skeleton.png'
---
Can you make a skeleton layout component to match a card, called SkeletonCard? Then add it to the /preview page so we can see it in a browser.
---